= render :template => "partials/_html.haml"
= render :template => "partials/_head.haml"
= render :template => "partials/_webfonts.haml"

%body
  #page
    %header
    #main{ :role => "main" }
      = yield
    %footer

= render :template => "partials/_frameworks.haml"
= render :template => "partials/_footer.haml"
