- @title = "Common Markup"
- @subnav = "<nav class='section-nav'></nav> "

= render :template => "/setup/common-elements.haml"

/ Common HTML Elements
%section#common.element-group
  %h1.section-title Common Elements
  -if @headers
    = render :template => "/survivalkit/partials/common/_headers.haml"
  -if @characters
    = render :template => "/survivalkit/partials/common/_characters.haml"
  -if @paragraphs
    = render :template => "/survivalkit/partials/common/_paragraphs.haml"
  -if @links
    = render :template => "/survivalkit/partials/common/_links.haml"
  -if @lists
    = render :template => "/survivalkit/partials/common/_lists.haml"
  -if @pre
    = render :template => "/survivalkit/partials/common/_pre.haml"
/ Phrase-level HTML Elements
%section#phrase-elements.element-group
  %h1.section-title Phrase Elements
  -if @em
    = render :template => "/survivalkit/partials/common/_em.haml"
  -if @strong
    = render :template => "/survivalkit/partials/common/_strong.haml"
  -if @cite
    = render :template => "/survivalkit/partials/common/_cite.haml"
  -if @dfn
    = render :template => "/survivalkit/partials/common/_dfn.haml"
  -if @code
    = render :template => "/survivalkit/partials/common/_code.haml"
  -if @samp
    = render :template => "/survivalkit/partials/common/_samp.haml"
  -if @kbd
    = render :template => "/survivalkit/partials/common/_kbd.haml"
  -if @var
    = render :template => "/survivalkit/partials/common/_var.haml"
  -if @abbr
    = render :template => "/survivalkit/partials/common/_abbr.haml"
  -if @acronym
    = render :template => "/survivalkit/partials/common/_acronym.haml"
  -if @q
    = render :template => "/survivalkit/partials/common/_q.haml"
  -if @others
    = render :template => "/survivalkit/partials/common/_others.haml"

