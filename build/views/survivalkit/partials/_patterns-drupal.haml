= render :template => "/setup/ui-patterns-drupal.haml"

%section#drupal-patterns.element-group
  %h1.section-title Drupal Patterns
  -if @site_name
    = render :template => "/survivalkit/partials/patterns-drupal/_site-name.haml"
  -if @search
    = render :template => "/survivalkit/partials/patterns-drupal/_search.haml"
  -if @messages
    = render :template => "/survivalkit/partials/patterns-drupal/_messages.haml"
  -if @block
    = render :template => "/survivalkit/partials/patterns-drupal/_block.haml"
  -if @tables
    = render :template => "/survivalkit/partials/patterns-drupal/_tables.haml"
  -if @rss_link
    = render :template => "/survivalkit/partials/patterns-drupal/_rss-link.haml"
  -if @links
    = render :template => "/survivalkit/partials/patterns-drupal/_links.haml"
  -if @taxonomy
    = render :template => "/survivalkit/partials/patterns-drupal/_taxonomy.haml"
  -if @field_image
    = render :template => "/survivalkit/partials/patterns-drupal/_field-image.haml"
  -if @node
    = render :template => "/survivalkit/partials/patterns-drupal/_node.haml"
  -if @comments
    = render :template => "/survivalkit/partials/patterns-drupal/_comments.haml"
  -if @form_comment
    = render :template => "/survivalkit/partials/patterns-drupal/_form-comment.haml"
%section#drupal-navigation.element-group
  %h1.section-title Drupal Navigation
  -if @menu
    = render :template => "/survivalkit/partials/patterns-drupal/_menu.haml"
  -if @menu_tree
    = render :template => "/survivalkit/partials/patterns-drupal/_menu-tree.haml"
  -if @tabs
    = render :template => "/survivalkit/partials/patterns-drupal/_tabs.haml"
  -if @breadcrumbs
    = render :template => "/survivalkit/partials/patterns-drupal/_breadcrumbs.haml"
  -if @pager
    = render :template => "/survivalkit/partials/patterns-drupal/_pager.haml"
%section#drupal-forms.element-group
  %h1.section-title Drupal Forms
  -if @form_basic
    = render :template => "/survivalkit/partials/patterns-drupal/_form-basic.haml"
  -if @form_submit
    = render :template => "/survivalkit/partials/patterns-drupal/_form-submit.haml"
  -if @form_button
    = render :template => "/survivalkit/partials/patterns-drupal/_form-button.haml"
  -if @form_image_button
    = render :template => "/survivalkit/partials/patterns-drupal/_form-image-button.haml"
  -if @form_markup
    = render :template => "/survivalkit/partials/patterns-drupal/_form-markup.haml"
  -if @form_fieldsets
    = render :template => "/survivalkit/partials/patterns-drupal/_form-fieldsets.haml"
  -if @form_vertical_tabs
    = render :template => "/survivalkit/partials/patterns-drupal/_form-vertical-tabs.haml"
  -if @form_new_account
    = render :template => "/survivalkit/partials/patterns-drupal/_form-new-account.haml"
  -if @form_login
    = render :template => "/survivalkit/partials/patterns-drupal/_form-login.haml"
  -if @form_new_password
    = render :template => "/survivalkit/partials/patterns-drupal/_form-new-password.haml"
